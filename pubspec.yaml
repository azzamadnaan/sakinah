name: sakinah
description: منصة إسلامية متكاملة ومساعد ذكي للورد اليومي ومواقيت الصلاة.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  geolocator: ^13.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # إعداد الخطوط (تأكد من إضافة ملفات الخطوط في مجلد assets/fonts إذا أردت استخدام خط Cairo)
  # fonts:
  #   - family: Cairo
  #     fonts:
  #       - asset: assets/fonts/Cairo-Regular.ttf
  #       - asset: assets/fonts/Cairo-Bold.ttf
  
